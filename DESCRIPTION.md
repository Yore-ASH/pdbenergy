# pdbenergy — 项目说明

> **一句话**：用 PyTorch 图神经网络从 PDB 文件直接预测蛋白质构象能量；
> 物理力场（AMBER14 + GBn2）只负责生成训练标签，神经网络学会复现它。

---

## 摘要

本项目搭建了一条从实验结构到机器学习势函数的完整流水线：
从 RCSB 下载真实 PDB 结构 → 用 OpenMM 的 AMBER14 全原子力场加 GBn2 隐式溶剂
做体系准备与能量标注 → 通过二面角旋转、NMR 沉积模型、朗之万分子动力学三种方式
生成构象系综 → 把结构表示成**距离图** → 用 **SchNet 消息传递网络**学习
结构到相对构象能 $\Delta E = E - \min(E)$ 的映射。

在 14 个蛋白、730 个构象上，按**蛋白质**切分（测试集蛋白训练中从未出现），
模型的测试集 Spearman 相关为 **0.43**，能在真实 NMR 条目上给出**正确**的能量排序，
但绝对精度**尚不足以充当打分函数**：预测值只覆盖真实能量跨度的 **14%**，
MAE 未超过常数基线。项目同时给出了完整的失败诊断与改进方向。

> 这份说明是给"要不要用/要不要读"的人看的。
> 想看**怎么做的、为什么这么做、踩过哪些坑**，请读 [`TeachFlow.md`](TeachFlow.md)（13 章，
> 从 PDB 文件格式一直讲到评估方法学）。

---

## 它解决什么问题

计算一个蛋白质构象的能量，需要在力场下遍历所有原子对；要探索构象空间还得跑分子动力学，
每一步都要重算。本项目实测（1CRN，642 原子，AMBER14+GBn2，8 线程 CPU）：

| 操作 | 无截断 | 1.0 nm 截断 |
|---|---|---|
| 单点能 | 65.6 ms | 22.0 ms |
| MD 每一步 | 80.5 ms | 22.2 ms |
| L-BFGS 最小化 50 步 | 23.8 s | 6.1 s |

训练好的网络做一次预测只需**毫秒级**，且可批量并行、可自动微分（从而免费得到力）。
这就是 **MLIP（机器学习势函数）** 的思路，代表性工作有 ANI-1、SchNet、NequIP、MACE。

**预测的是相对构象能** $\Delta E = E - \min(E)$，而不是绝对值：
绝对能量主要由原子组成和长程溶剂化决定，跨分子不可比；
同一分子的不同构象之间比较才有物理意义——这正是打分函数关心的问题。

---

## 流水线

```
RCSB PDB 文件
   │  ① 下载 + 体检                       prepare.py
   ▼
干净的全原子结构（PDBFixer 加氢、补原子，pH 7.0）
   │  ② 力场参数化 + 能量最小化             labels.py
   ▼
OpenMM System + 参考能量 E_min
   │  ③ 构象采样：二面角旋转 / NMR / MD     ensemble.py
   ▼
(构象, ΔE) 样本对 + 冲突筛选
   │  ④ 按蛋白切分 + 目标标准化             dataset.py
   ▼
train / val / test（按蛋白质，互斥完整）
   │  ⑤ 结构 → 图（距离 + 共价键 + 截断包络）features.py
   ▼
图数据 + 磁盘缓存
   │  ⑥ SchNet 消息传递 + 求和池化          models.py / train.py
   ▼
预测 ΔE
   │  ⑦ 指标、分组分解、排序能力、图表       evaluate.py
   ▼
评估报告 + 泄漏消融
```

### 三种构象来源，各覆盖一段能量区间

| 来源 | 生成方式 | 覆盖的物理区域 |
|---|---|---|
| `nmr` | NMR 沉积模型（每个重新加氢 + 放松） | 实验观测构象 |
| `torsion` | 绕可旋转单键的**二面角旋转** | 低到中高能，无键长应变 |
| `md300K` / `md450K` | 朗之万动力学 | 热可及构象（含均分能量偏置） |

二面角旋转用**罗德里格斯公式**绕键轴旋转一侧子树，**精确保持所有键长与键角**，
唯一变化的就是那个二面角。这比给坐标加高斯噪声正确得多——后者在 0.04 Å 的幅度下
就会引入约 800 kcal/mol 的纯键长拉伸噪声（本项目有实测对比）。

---

## 关键技术决策（都有实测依据）

| 决策 | 选择 | 依据（实测） |
|---|---|---|
| 溶剂模型 | GBn2 隐式溶剂 + 1.0 nm 截断 | 比精确 `NoCutoff` 快 **3.6 倍**，且是 GB 模拟的惯例 |
| 构象扰动 | 二面角旋转，**不用**笛卡尔噪声 | 0.04 Å 噪声 → ΔE 中位数 797 kcal/mol（键长噪声） |
| 冲突处理 | 逐帧能量筛选，超阈值丢弃 | 未筛选时 ΔE 最大达 $5\times10^9$ kcal/mol（LJ $r^{-12}$ 爆炸） |
| MD 阈值 | 独立放宽到 1500 kcal/mol | 均分定理：300 K 下 640 原子蛋白的势能本就比极小值高约 570 kcal/mol |
| NMR 处理 | **每个模型重新加氢**，不搬运氢 | 搬运氢会让 C–H 键被拉开近 1 Å → 保留率 0/38 |
| 边特征 | 只存距离 + 键标记，RBF 在网络内展开 | 缓存缩小约 25 倍；RBF 是稀疏的（每个距离只点亮 3–4 个基函数） |
| 图缓存 | 特征化一次，存盘复用 | 单帧 150 ms × 750 帧 × 80 轮 = 2.5 小时纯重复计算 |
| 数据切分 | **按蛋白质**切分 | 按帧切分时 53% 的"测试集"是训练集的近似副本 |
| 输出层初始化 | 读出头最后一层**置零** | 未置零时首轮验证 MAE 702 kcal/mol（比常数预测器还差 6 倍） |
| 损失函数 | Huber（δ=1） | 目标跨度 0–1200 kcal/mol，离群点会主导 MSE 的梯度 |

---

## 实测结果（诚实版）

数据集：14 个蛋白、730 个构象，6 训练 / 4 验证 / 4 测试（按蛋白质）。

| 模型 | 参数量 | 测试 MAE | 测试 RMSE | 测试 R² | 测试 Spearman | 常数基线 MAE |
|---|---|---|---|---|---|---|
| SchNet（图神经网络） | 42,562 | 121.06 | 161.26 | 0.062 | **0.431** | 107.29 |
| MLP（手工描述符基线） | 9,985 | 105.97 | 166.67 | -0.002 | — | 107.29 |

单位 kcal/mol。

**按采样来源分解（SchNet，测试集）**

| 来源 | 样本数 | MAE | Spearman |
|---|---|---|---|
| `nmr` | 3 | 68.23 | 0.500 |
| `torsion` | 181 | 86.66 | 0.360 |
| `md300K` | 12 | 279.79 | -0.189 |
| `md450K` | 12 | 494.42 | 0.720 |

**结论**

* ✅ **流水线完整可用**：全流程跑通，产物齐全（检查点、指标、24 张图）。
* ✅ **带真实排序信号**：全局 Spearman 0.43；在真实 `1L2Y.pdb` 上，
  模型把三个 NMR 模型排成了与力场真值一致的能量顺序（0 < 39 < 256 kcal/mol）。
* ❌ **绝对精度不够**：预测区间只有 84–186 kcal/mol，真实跨度 0–709，**仅覆盖 14%**，
  典型的"回归到均值"。
* ❌ **高能区最差**：`md450K` 的 MAE 是 `torsion` 的 **5.7 倍**。

**诊断**：误差集中在高温 MD 帧，其能量主要来自键长/键角的热形变
（300–450 K 下约 0.05–0.1 Å）。而本项目的径向基间距是 **0.161 Å**——
**特征分辨率比要检测的信号还粗**。叠加只有 323 个训练构象（真实 MLIP 数据集是
$10^5$–$10^7$ 量级），模型学不动高能区是必然的。具体的改进路径见 TeachFlow §12.3。

---

## 数据泄漏消融（本项目最值得记住的一个数字）

用"训练两次比 MAE"来论证数据泄漏是**间接**的——差距大小取决于模型有没有能力
记住近似副本。本项目改为**直接测量**训练集与测试集的重叠（不需要训练任何模型）：

| 切分方式 | 测试帧最近邻同蛋白比例 | 最近邻距离中位数 | 近似重复比例 (<0.05) |
|---|---|---|---|
| 按蛋白质切分（正确） | **0.0%** | 4.775 | 0.0% |
| 按帧随机切分（常见错误） | **97.3%** | 0.044 | **52.7%** |

按帧随机切分时，**97% 的测试帧在训练集里的最近邻来自同一个蛋白，一半以上是近似重复**。
用 MAE 差距看这件事会被误导（本项目用低容量 MLP 做消融时差距只有 0.95 倍）。

---

## 快速开始

```powershell
git clone <仓库地址> && cd pdbenergy
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[physics]"
.\.venv\Scripts\python.exe -m pip install torch --index-url https://download.pytorch.org/whl/cpu

pdbenergy inventory                              # 看看数据
pdbenergy dataset                                # 仓库已含已标注数据集，可跳过 ensemble
pdbenergy train --model schnet
pdbenergy evaluate --run-dir outputs/schnet_protein
pdbenergy predict data/raw/1L2Y.pdb --verify     # 需要先 download
```

调用方式（命令行 / 现成脚本 / Python 库）详见 [`examples/`](examples/) 与 [README](README.md)。

---

## 项目结构

```
pdbenergy/          15 个模块：PDB 解析、力场引擎、构象采样、特征、模型、训练、评估、推理、CLI
examples/           三种调用方式的可运行示例
configs/            default.json / quick.json（--config 加载）
scripts/            run_pipeline.ps1（一键全流程）、write_results.py（由产物生成结果章节）
benchmarks/         物理与模型开销的实测脚本 + 实测数据表
tests/              58 个单元测试
outputs/            检查点、指标 JSON、24 张图
TeachFlow.md        13 章从零开始的完整教程（1771 行）
```

---

## 测试

```powershell
python -m unittest discover -s tests -v          # 全部 58 个（物理测试需 OpenMM，约 4.5 分钟）
python -m unittest tests.test_pdbio tests.test_ml  # 不需要 OpenMM（秒级）
```

覆盖的性质包括：`TER` 记录只在链结束处出现、原子置换/整体旋转平移不改变模型输出、
二面角旋转**精确保持**键长键角、能量分解四项之和**等于**总能量、
按蛋白质切分互斥且完整、同种子可复现。

CI 分两个 job：`tests`（Python 3.11/3.13，纯 CPU torch，无 OpenMM、无网络）、
`physics`（装 OpenMM 并下载 1CRN 后跑力场自洽性测试）。

---

## 适合谁用

* **想学分子机器学习**：`TeachFlow.md` 是一份从物理讲到训练实践的完整教材，
  每一处都有代码对应，并且记录了所有踩过的坑。
* **想学"怎么做诚实的评估"**：第 9 章 + 数据泄漏直接测量，是通用方法学。
* **想拿去做骨架**：整条流水线（力场打标签 → 图表示 → GNN → 评估）可以直接换成
  别的体系（小分子、材料、蛋白-配体复合物）。
* **想直接用模型**：不适合。当前模型是**方法演示**，不是可用打分函数。

---

## 许可与引用

MIT License，见 [`LICENSE`](LICENSE)。

依赖的第三方工具与文献（OpenMM、AMBER、GBn2、PDBFixer、SchNet、ANI-1 等）
在 [`TeachFlow.md`](TeachFlow.md) 第 13 章有完整列表。
